# Whaletown Flows Hub

🐋 一个纯前端的多功能图片处理工具集合，集成了 AI 抠图、格式转换、切割、矢量化、GIF 制作等功能。

## ✨ 特性

- **🎯 多工作流聚合**：单页 Tab 切换，每个工作流仅创建一次，切换时不重载，保持状态
- **📋 全局剪贴板**：不同工具间无缝共享图片数据，支持批量操作
- **🚀 纯前端运行**：无需后端服务器，完全在浏览器本地处理
- **💾 零安装**：开箱即用，只需启动静态服务器
- **🎨 拖拽上传**：所有工具支持拖拽文件上传

## 🛠️ 工作流工具

### 1. RMBG 抠图 (`rmbg/`)
- 基于 BRIA RMBG 1.4 AI 模型
- 使用 ONNX + WebAssembly 在浏览器内实现智能抠图
- 完全本地运行，保护隐私
- 支持高质量前景提取

### 2. 多图切割 (`slicer/`)
- 网格切割工具，自定义行列数
- 可调整边距、间距、内边距
- 支持单张下载或批量导出 ZIP
- 可将切割结果生成 GIF 动画
- 格式支持：PNG / JPG

### 3. 图片矢量化 (`image2bitmap/`)
- 基于 ImageTracer.js 将位图转换为 SVG
- 适合制作可无限缩放的矢量图形
- 支持下载 SVG 文件或发送到剪贴板

### 4. 格式转换 (`convert/`)
- 支持 PNG / JPG / WebP / ICO 互转
- ICO 可生成多尺寸（16×16 到 512×512）
- 可调整 JPEG / WebP 压缩质量
- 实时预览转换结果和文件大小对比

### 5. GIF 制作 (`gif/`)
- 多图合成 GIF 动画
- 支持拖拽排序帧顺序
- 可设置帧延迟、播放顺序（正序/倒序/往返）
- 支持从 ZIP 批量导入图片
- 可调整 GIF 质量和尺寸

## 📋 剪贴板系统

全局剪贴板允许不同工具间共享图片数据：

- **添加到剪贴板**：每个工具处理完成后可发送结果到剪贴板
- **从剪贴板导入**：点击"从剪贴板导入"按钮快速获取图片
- **点击粘贴**：直接点击剪贴板中的项目粘贴到当前工具
- **批量操作**：支持一次性添加多张图片（如切割结果、ICO 多尺寸）
- **会话保存**：剪贴板数据在会话期间保持，最多保存 50 个项目

**工作流示例：**
```
抠图工具去背景 → 发送到剪贴板 → 格式转换工具导入 → 转换为 WebP → 发送到剪贴板 → GIF 制作工具导入
```

## 🚀 快速开始

### 1. 启动静态服务器

```bash
# 使用 Python
python3 -m http.server 8000

# 或使用 Node.js
npx serve
```

### 2. 访问应用

在浏览器打开 `http://localhost:8000/`

### 3. 开始使用

- 点击顶部 Tab 切换不同工具
- 拖拽或点击上传图片
- 处理完成后可下载或发送到剪贴板
- 在其他工具中导入剪贴板内容继续处理

## 🔧 技术栈

- **核心**：HTML5 + CSS3 + JavaScript (ES6+)
- **AI 推理**：ONNX Runtime Web
- **图片处理**：Canvas API
- **第三方库**：
  - ImageTracer.js - 矢量化
  - gifshot.js - GIF 生成
  - JSZip - ZIP 打包

## 📦 添加新工作流

1. 在项目根目录创建新的工作流文件夹，例如 `./myflow/`
2. 在文件夹中创建 `index.html`、`app.js`、`styles.css`
3. 在 `app.js` 的 `flows` 数组中添加配置：

```javascript
{
  id: 'myflow',
  name: '我的工具',
  description: '工具描述',
  url: './myflow/' // 注意末尾斜杠
}
```

4. 刷新页面即可看到新的 Tab

### 与剪贴板集成（可选）

在你的工具中添加剪贴板支持：

```javascript
// 发送到剪贴板
window.parent.clipboardAdd({
  name: 'result.png',
  url: blobURL,
  blob: blob,
  type: 'image/png',
  source: '我的工具',
  metadata: { /* 自定义元数据 */ }
})

// 从剪贴板获取
const items = window.parent.clipboardGetAll()

// 监听剪贴板粘贴消息
window.addEventListener('message', (event) => {
  if (event.data.type === 'clipboard-paste') {
    const item = event.data.item
    // 处理粘贴的项目
  }
})
```